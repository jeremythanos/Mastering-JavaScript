"use strict"
/*
const ubicacion = navigator.geolocation;

const posicion = (pos)=>{
    console.log(pos.coords.longitude)
}

ubicacion.getCurrentPosition(posicion)
*/

// history (para no crear otro archivo jajaj)
console.log(history);
